# Model
model_name: distilbert-base-uncased
num_labels: 2

# Data
dataset: imdb
max_length: 128
train_size: 5000
eval_size: 500

# Training
batch_size: 16
learning_rate: 0.00002
num_epochs: 1
seed: 42

# Output
output_dir: ./checkpoints/run-data5000
